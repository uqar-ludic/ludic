@model IEnumerable<SiteSandBox.Models.Save>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Solution)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExerceId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Solution)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExerceId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SaveId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SaveId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SaveId })
        </td>
    </tr>
}

</table>
