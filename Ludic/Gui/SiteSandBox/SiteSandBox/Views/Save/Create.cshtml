@model SiteSandBox.Models.Save

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Save</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Solution)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Solution)
            @Html.ValidationMessageFor(model => model.Solution)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ExerceId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ExerceId)
            @Html.ValidationMessageFor(model => model.ExerceId)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
